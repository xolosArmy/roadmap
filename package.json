{
  "name": "@xolosarmy/public-roadmap",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A validated public derivative of the xolosArmy operational roadmap.",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "prepare:contract": "node scripts/compile-contract.mjs",
    "lint": "eslint src tests scripts && node scripts/check-generated.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "test:bundle": "node --test tests/bundle.integration.ts",
    "validate:snapshot": "node scripts/validate-snapshots.ts",
    "publish:snapshot": "node scripts/publish.ts",
    "build": "node scripts/build.ts",
    "serve": "node scripts/serve.mjs",
    "check": "npm run prepare:contract && npm run lint && npm run typecheck && npm test && npm run validate:snapshot && npm run build && npm run test:bundle"
  },
  "devDependencies": {
    "@types/jsdom": "30.0.0",
    "@types/node": "24.13.6",
    "ajv": "8.20.0",
    "esbuild": "0.28.2",
    "eslint": "10.11.0",
    "jsdom": "30.1.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0"
  }
}
